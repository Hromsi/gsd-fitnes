import { EmptyState } from "@/components/ui/empty-state";

export default function AppHomePage() {
  return (
    <EmptyState
      title="Your workout profile is ready"
      body="Next up: generate your first workout plan."
    />
  );
}
