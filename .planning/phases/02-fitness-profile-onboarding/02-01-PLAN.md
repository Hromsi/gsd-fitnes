---
phase: 02-fitness-profile-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/0002_profile_onboarding_fields/migration.sql
  - lib/validators/profile.ts
  - app/onboarding/page.tsx
  - app/(app)/settings/profile/page.tsx
  - app/(app)/settings/page.tsx
  - components/profile/profile-form.tsx
  - components/profile/option-group.tsx
  - app/(app)/actions/profile.ts
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
must_haves:
  truths:
    - A signed-in user can enter and save all four required profile inputs through one onboarding form.
    - The same form surface can be reused later in edit mode with the user’s current values prefilled.
    - Validation rules for goal, level, equipment, and frequency are centralized and strictly constrained to the approved MVP values.
  artifacts:
    - lib/validators/profile.ts
    - app/onboarding/page.tsx
    - app/(app)/settings/profile/page.tsx
    - components/profile/profile-form.tsx
    - app/(app)/actions/profile.ts
  key_links:
    - app/onboarding/page.tsx and app/(app)/settings/profile/page.tsx both use the same profile form and validation contract.
    - app/(app)/actions/profile.ts persists data that matches lib/validators/profile.ts and prisma/schema.prisma.
---

<objective>
Implement the actual fitness-profile data capture flow for the MVP.

Purpose: Turn the placeholder onboarding route into a real single-page profile form that stores the deterministic plan inputs.
Output: Real onboarding/profile form UI, strict validation, and save/update actions for the signed-in user's profile.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fitness-profile-onboarding/02-CONTEXT.md
@.planning/phases/02-fitness-profile-onboarding/02-RESEARCH.md
@.planning/phases/02-fitness-profile-onboarding/02-UI-SPEC.md
@.planning/phases/01-foundation-and-auth/01-02-SUMMARY.md
@.planning/phases/01-foundation-and-auth/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the profile model and create a shared validation contract</name>
  <files>prisma/schema.prisma, prisma/migrations/0002_profile_onboarding_fields/migration.sql, lib/validators/profile.ts</files>
  <read_first>prisma/schema.prisma, lib/validators/profile.ts, .planning/phases/02-fitness-profile-onboarding/02-CONTEXT.md, .planning/phases/02-fitness-profile-onboarding/02-RESEARCH.md</read_first>
  <action>Update the existing `Profile` model to support the finalized onboarding requirements cleanly. Store the four required inputs in a way that is explicit and safe for later plan generation: `goal`, `fitnessLevel`, `equipment`, and `trainingFrequency`, all required once onboarding is complete and limited to the approved values. Add a new migration `prisma/migrations/0002_profile_onboarding_fields/migration.sql` that evolves the Phase 1 placeholder profile structure into this stricter onboarding-ready shape without introducing unrelated models. Create `lib/validators/profile.ts` with a shared schema and exported option definitions/enums for the exact values locked in discussion: `lose_weight`, `build_muscle`, `maintain`; `beginner`, `intermediate`; `none`, `home`, `gym`; and training frequency `3`, `4`, `5` only.</action>
  <verify>test -f lib/validators/profile.ts && rg -n "lose_weight|build_muscle|maintain|beginner|intermediate|none|home|gym|3|4|5" lib/validators/profile.ts prisma/schema.prisma && test -f prisma/migrations/0002_profile_onboarding_fields/migration.sql</verify>
  <acceptance_criteria>
    - `lib/validators/profile.ts` contains the exact allowed values for all four profile fields.
    - `prisma/schema.prisma` contains onboarding-ready profile fields matching the validator contract.
    - `prisma/schema.prisma` still preserves one profile per user.
    - `prisma/migrations/0002_profile_onboarding_fields/migration.sql` exists.
  </acceptance_criteria>
  <done>The data model and validation layer are ready for a strict MVP onboarding form.</done>
</task>

<task type="auto">
  <name>Task 2: Build the shared onboarding/edit form and persistence action</name>
  <files>components/profile/profile-form.tsx, components/profile/option-group.tsx, app/(app)/actions/profile.ts, app/onboarding/page.tsx, app/(app)/settings/profile/page.tsx</files>
  <read_first>app/onboarding/page.tsx, components/profile/profile-form.tsx, app/(app)/actions/profile.ts, .planning/phases/02-fitness-profile-onboarding/02-UI-SPEC.md</read_first>
  <action>Create a reusable one-page profile form that follows the approved UI contract: one main card, constrained selection controls, minimal helper copy, and a single primary save action. Add `components/profile/profile-form.tsx` to render all four required fields and their current values. Add a small presentation helper such as `components/profile/option-group.tsx` if needed to keep field groups consistent and tappable on mobile. Implement `app/(app)/actions/profile.ts` as the server action that validates the submitted values, updates the signed-in user’s profile, and supports both first-time onboarding and later edits. Update `app/onboarding/page.tsx` to render the real onboarding form for incomplete users, and add `app/(app)/settings/profile/page.tsx` to reuse that same form in edit mode for completed users.</action>
  <verify>test -f components/profile/profile-form.tsx && test -f app/(app)/actions/profile.ts && test -f app/onboarding/page.tsx && test -f app/(app)/settings/profile/page.tsx && pnpm vitest run tests/profile/profile-form.test.tsx</verify>
  <acceptance_criteria>
    - `app/onboarding/page.tsx` contains the real profile form rather than placeholder copy.
    - `app/(app)/settings/profile/page.tsx` exists and renders the same profile editing surface.
    - `app/(app)/actions/profile.ts` validates and saves all four required fields.
    - Form fields are prefilled from saved profile values when editing.
    - The form does not introduce free-text inputs for the four locked onboarding values.
  </acceptance_criteria>
  <done>Users can create and edit their fitness profile through one shared form system.</done>
</task>

<task type="auto">
  <name>Task 3: Add settings entry points and keep the shell coherent</name>
  <files>app/(app)/settings/page.tsx, app/(app)/layout.tsx, components/layout/top-nav.tsx, app/onboarding/page.tsx</files>
  <read_first>app/(app)/layout.tsx, components/layout/top-nav.tsx, app/onboarding/page.tsx, .planning/phases/02-fitness-profile-onboarding/02-UI-SPEC.md</read_first>
  <action>Add a minimal settings/profile entry point inside the authenticated shell so completed users have a clear path back to editing their onboarding data. Update the relevant shell/navigation pieces to expose that profile/settings route without turning the app into a sidebar layout or dashboard-heavy shell. Keep the onboarding page and edit page visually aligned, preserve the existing top-nav direction from Phase 1, and avoid introducing any plan-generation UI in this step.</action>
  <verify>test -f app/(app)/settings/page.tsx && rg -n "Profile settings|/settings/profile" app/(app)/settings/page.tsx app/(app)/layout.tsx components/layout/top-nav.tsx app/onboarding/page.tsx && pnpm vitest run tests/profile/profile-settings-routing.test.ts</verify>
  <acceptance_criteria>
    - There is a dedicated profile/settings route users can revisit after onboarding.
    - The authenticated shell exposes a clear profile/settings entry point.
    - No sidebar navigation is introduced.
    - Onboarding and edit mode stay visually consistent with the Phase 2 UI contract.
  </acceptance_criteria>
  <done>The onboarding experience and later profile-edit entry point are connected inside the existing MVP shell.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` exits 0
- [ ] `pnpm build` exits 0
- [ ] Manual check confirms the onboarding form and edit form render with saved values
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can create and update a strict, required fitness profile through a single shared form system
</success_criteria>

<output>
After completion, create `.planning/phases/02-fitness-profile-onboarding/02-01-SUMMARY.md`
</output>
