---
phase: 02-fitness-profile-onboarding
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app/onboarding/page.tsx
  - app/(app)/layout.tsx
  - app/(app)/next-step/page.tsx
  - app/(app)/actions/profile.ts
  - lib/current-user.ts
  - tests/profile/profile-success-routing.test.ts
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
must_haves:
  truths:
    - Completing onboarding marks the user as setup-complete only after all four fields are valid and saved.
    - Users see a brief success state before landing on a simple post-onboarding placeholder for the upcoming plan-generation phase.
    - Returning completed users can still reach edit mode intentionally instead of being locked out of their own profile settings.
  artifacts:
    - app/onboarding/page.tsx
    - app/(app)/next-step/page.tsx
    - app/(app)/actions/profile.ts
    - tests/profile/profile-success-routing.test.ts
  key_links:
    - app/(app)/actions/profile.ts updates `setupCompleted` in the profile data model used by app/onboarding/page.tsx and app/(app)/layout.tsx.
    - app/onboarding/page.tsx and app/(app)/next-step/page.tsx encode the same handoff contract into the next phase without pretending the plan exists yet.
---

<objective>
Finalize onboarding completion behavior, success feedback, and post-save routing.

Purpose: Make Phase 2 end in a coherent user flow that cleanly hands off into future plan generation.
Output: Completion-state updates, success-state UX, next-step placeholder page, and routing rules for incomplete vs completed users.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fitness-profile-onboarding/02-CONTEXT.md
@.planning/phases/02-fitness-profile-onboarding/02-RESEARCH.md
@.planning/phases/02-fitness-profile-onboarding/02-UI-SPEC.md
@.planning/phases/02-fitness-profile-onboarding/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire explicit completion state and success feedback into onboarding save flow</name>
  <files>app/(app)/actions/profile.ts, app/onboarding/page.tsx, lib/current-user.ts</files>
  <read_first>app/(app)/actions/profile.ts, app/onboarding/page.tsx, lib/current-user.ts, .planning/phases/02-fitness-profile-onboarding/02-CONTEXT.md</read_first>
  <action>Update the profile save flow so onboarding completion is explicit and safe. Saving valid onboarding data should mark the profile as complete by setting `setupCompleted` only after all four required values pass validation and persist successfully. Reflect that status back into the page flow so first-time onboarding users see the approved short success state rather than an abrupt jump. Ensure the current-user/profile fetch helpers expose enough state for the onboarding route to distinguish incomplete, just-completed, and already-completed users without introducing duplicated authorization logic.</action>
  <verify>rg -n "setupCompleted|Your profile is ready|Next up: generate your first workout plan" app/(app)/actions/profile.ts app/onboarding/page.tsx lib/current-user.ts && pnpm vitest run tests/profile/profile-success-routing.test.ts</verify>
  <acceptance_criteria>
    - `app/(app)/actions/profile.ts` only marks setup complete after valid save.
    - `app/onboarding/page.tsx` contains the short success-state copy from the UI contract.
    - Shared user/profile resolution still happens through secure server-side current-user helpers.
  </acceptance_criteria>
  <done>The onboarding save flow now has explicit completion semantics and success feedback.</done>
</task>

<task type="auto">
  <name>Task 2: Add the post-onboarding placeholder route and route users there intentionally</name>
  <files>app/(app)/next-step/page.tsx, app/onboarding/page.tsx, app/(app)/layout.tsx</files>
  <read_first>app/(app)/next-step/page.tsx, app/onboarding/page.tsx, app/(app)/layout.tsx, .planning/phases/02-fitness-profile-onboarding/02-UI-SPEC.md</read_first>
  <action>Create a simple authenticated placeholder page such as `app/(app)/next-step/page.tsx` that communicates the next product milestone: workout-plan generation comes next. After the short success state, route newly completed users there instead of to an empty dashboard or nonexistent plan page. Update layout or routing logic as needed so incomplete users still go to onboarding first, while completed users can access the rest of the protected shell and the new next-step placeholder without being bounced back unnecessarily.</action>
  <verify>test -f app/(app)/next-step/page.tsx && rg -n "generate your first workout plan|next step|onboarding" app/(app)/next-step/page.tsx app/onboarding/page.tsx app/(app)/layout.tsx && pnpm vitest run tests/profile/profile-success-routing.test.ts</verify>
  <acceptance_criteria>
    - A post-onboarding placeholder route exists.
    - The placeholder copy clearly signals plan generation as the next step.
    - Newly completed users are routed there after success feedback.
    - Incomplete users are still kept on onboarding when they try to access protected content.
  </acceptance_criteria>
  <done>Phase 2 ends on a coherent next-step screen instead of a dead end.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve edit-mode access for completed users and tighten automated verification</name>
  <files>app/onboarding/page.tsx, app/(app)/settings/profile/page.tsx, tests/profile/profile-success-routing.test.ts, tests/profile/profile-form.test.tsx</files>
  <read_first>app/onboarding/page.tsx, app/(app)/settings/profile/page.tsx, tests/profile/profile-success-routing.test.ts, tests/profile/profile-form.test.tsx, .planning/phases/02-fitness-profile-onboarding/02-CONTEXT.md</read_first>
  <action>Refine routing so completed users are not permanently blocked from their own onboarding data: they should have an intentional edit-mode path through the dedicated profile/settings route, while the onboarding route itself can either support edit mode directly or delegate clearly to settings in a way that matches the locked decisions. Add or update test coverage to verify required-value validation, completion save semantics, the post-onboarding destination, and completed-user edit access. Keep the logic aligned with the product decision that users may update their profile later without turning onboarding into a looping wizard.</action>
  <verify>pnpm vitest run tests/profile/profile-form.test.tsx tests/profile/profile-success-routing.test.ts && rg -n "settings/profile|setupCompleted|next-step" app/onboarding/page.tsx app/(app)/settings/profile/page.tsx</verify>
  <acceptance_criteria>
    - Completed users have a working edit path through dedicated settings/profile UI.
    - Tests cover completion routing and edit-mode access.
    - The phase does not introduce any plan-generation business logic yet.
  </acceptance_criteria>
  <done>The onboarding flow is complete, test-backed, and ready to hand off to Phase 3.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` exits 0
- [ ] `pnpm vitest run` exits 0
- [ ] Manual check confirms save → success state → next-step routing and later profile editing both work
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Completing onboarding produces a valid, editable profile and a clean handoff into the upcoming plan-generation phase
</success_criteria>

<output>
After completion, create `.planning/phases/02-fitness-profile-onboarding/02-02-SUMMARY.md`
</output>
