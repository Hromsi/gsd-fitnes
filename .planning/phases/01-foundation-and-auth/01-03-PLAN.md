---
phase: 01-foundation-and-auth
plan: 03
type: execute
wave: 4
depends_on:
  - "01-01"
  - "01-02"
  - "01-04"
files_modified:
  - app/(public)/page.tsx
  - app/(app)/layout.tsx
  - components/layout/top-nav.tsx
  - components/layout/page-shell.tsx
  - components/auth/auth-card.tsx
  - components/ui/empty-state.tsx
  - lib/user-ownership.ts
  - app/(public)/login/page.tsx
  - app/(public)/signup/page.tsx
autonomous: true
requirements:
  - AUTH-03
  - UX-02
user_setup: []
must_haves:
  truths:
    - Logged-out users see a minimal landing page with clear login/signup actions.
    - Signed-up users are routed directly into onboarding/profile setup rather than a dashboard placeholder.
    - Authenticated pages use a minimal top navigation shell and shared ownership-safe helpers.
  artifacts:
    - app/(public)/page.tsx
    - app/onboarding/page.tsx
    - components/layout/top-nav.tsx
    - components/layout/page-shell.tsx
    - lib/user-ownership.ts
  key_links:
    - signup flow redirects into the onboarding route created in 01-02 and refined through shared shell styling here.
    - top-nav and protected layout expose sign-out access without introducing sidebar navigation.
---

<objective>
Build the minimal public/app shell and ownership-safe scaffolding that completes the Phase 1 user experience.

Purpose: Turn the auth foundation into a coherent MVP entry flow for logged-out and newly signed-up users.
Output: Landing page, authenticated top-nav shell, and shared ownership helper patterns for future phases.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-auth/01-CONTEXT.md
@.planning/phases/01-foundation-and-auth/01-RESEARCH.md
@.planning/phases/01-foundation-and-auth/01-UI-SPEC.md
@.planning/phases/01-foundation-and-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-auth/01-02-SUMMARY.md
@.planning/phases/01-foundation-and-auth/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the public landing page and reusable auth-shell components</name>
  <files>app/(public)/page.tsx, components/auth/auth-card.tsx, components/ui/empty-state.tsx, components/layout/page-shell.tsx</files>
  <read_first>app/(public)/page.tsx, components/auth/auth-card.tsx, components/ui/empty-state.tsx, components/layout/page-shell.tsx, .planning/phases/01-foundation-and-auth/01-UI-SPEC.md, .planning/phases/01-foundation-and-auth/01-CONTEXT.md</read_first>
  <action>Create the public landing page at `app/(public)/page.tsx` using the UI-SPEC contract: plain-language display headline as the focal point, `Create account` as the primary CTA, `Log in` as the secondary action, warm neutral palette, and MVP-minimal copy rather than marketing-heavy sections. Create reusable presentation components where helpful: `components/layout/page-shell.tsx` for page spacing/canvas rules, `components/auth/auth-card.tsx` for shared auth-card structure, and `components/ui/empty-state.tsx` for simple empty-state messaging that can be reused in onboarding or protected placeholders. Keep the component set intentionally small and Phase 1-scoped.</action>
  <verify>test -f "app/(public)/page.tsx" && rg -n "Create account|Log in|Personalized Workout Planner" "app/(public)/page.tsx" && pnpm vitest run tests/ui/landing-page.test.tsx</verify>
  <acceptance_criteria>
    - `app/(public)/page.tsx` contains `Create account` and `Log in`.
    - `app/(public)/page.tsx` contains a plain-language headline describing the workout-planner value.
    - `components/auth/auth-card.tsx` exists and provides a reusable auth-card shell.
    - `components/layout/page-shell.tsx` exists and encodes shared spacing/surface layout rules.
  </acceptance_criteria>
  <done>The app has a minimal but intentional public entry screen and reusable shell components.</done>
</task>

<task type="auto">
  <name>Task 2: Build the authenticated top-nav shell and onboarding placeholder route</name>
  <files>app/(app)/layout.tsx, components/layout/top-nav.tsx</files>
  <read_first>app/(app)/layout.tsx, components/layout/top-nav.tsx, app/onboarding/page.tsx, lib/auth.ts, lib/current-user.ts, .planning/phases/01-foundation-and-auth/01-UI-SPEC.md</read_first>
  <action>Update `app/(app)/layout.tsx` to render the authenticated shell using a minimal top nav only, with app identity, current destination context where helpful, and sign-out access. Keep the locked D-10 behavior intact by preserving the redirect of incomplete users back to `/onboarding` whenever they try to open other protected app routes. Create `components/layout/top-nav.tsx` that follows the approved color, spacing, and accent rules without introducing sidebar navigation. Refine the already-created onboarding placeholder experience only as needed through shared shell styling; do not move ownership of the route itself into this plan.</action>
  <verify>test -f "app/onboarding/page.tsx" && test -f "components/layout/top-nav.tsx" && test -f "app/(app)/layout.tsx" && pnpm vitest run tests/ui/onboarding-shell.test.tsx</verify>
  <acceptance_criteria>
    - `components/layout/top-nav.tsx` exists and contains sign-out access.
    - `app/(app)/layout.tsx` uses the top-nav component and does not contain sidebar layout markup.
    - `app/(app)/layout.tsx` contains redirect logic that keeps incomplete users on `/onboarding`.
    - The onboarding route is protected by the authenticated app shell.
  </acceptance_criteria>
  <done>Authenticated users land inside a minimal app shell that respects the existing onboarding-first route behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add ownership-safe helper patterns and responsive flow polish</name>
  <files>lib/user-ownership.ts, app/onboarding/page.tsx, app/(public)/login/page.tsx, app/(public)/signup/page.tsx, components/layout/top-nav.tsx</files>
  <read_first>lib/user-ownership.ts, app/onboarding/page.tsx, "app/(public)/login/page.tsx", "app/(public)/signup/page.tsx", components/layout/top-nav.tsx, .planning/phases/01-foundation-and-auth/01-CONTEXT.md, .planning/phases/01-foundation-and-auth/01-UI-SPEC.md</read_first>
  <action>Create `lib/user-ownership.ts` with helper functions or conventions that enforce future user-scoped queries through current-user resolution rather than ad hoc ownership checks. Apply responsive polish to the landing, login, signup, the already-created onboarding route, and top-nav experiences so mobile screens preserve the single-column layout, 44px tap-target rule, and CTA hierarchy defined in the UI-SPEC. Make sure the sign-out confirmation copy and empty-state language from the UI contract are available in the shell or placeholder patterns where appropriate.</action>
  <verify>test -f lib/user-ownership.ts && rg -n "44px|Create account|Sign out|No account session yet" lib/user-ownership.ts app/onboarding/page.tsx "app/(public)/login/page.tsx" "app/(public)/signup/page.tsx" components/layout/top-nav.tsx app/(public)/page.tsx && pnpm vitest run tests/ui/responsive-shell.test.tsx</verify>
  <acceptance_criteria>
    - `lib/user-ownership.ts` exists and contains reusable ownership-safe helper logic or conventions.
    - Landing/auth/onboarding/top-nav files contain responsive or layout classes consistent with single-column mobile behavior.
    - At least one relevant UI file contains `Sign out` confirmation wording or integrates the approved destructive copy.
    - No file in this plan introduces a sidebar or dashboard-first redirect.
  </acceptance_criteria>
  <done>The Phase 1 shell is cohesive, mobile-safe, and prepared for user-owned data while refining the existing onboarding experience.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` or equivalent exits 0
- [ ] Manual responsive check confirms landing, login, signup, and onboarding work on narrow/mobile widths
- [ ] Signup flow ends at `/onboarding` inside the authenticated shell
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Phase 1 public and protected UX matches the locked MVP contract
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-auth/01-03-SUMMARY.md`
</output>
