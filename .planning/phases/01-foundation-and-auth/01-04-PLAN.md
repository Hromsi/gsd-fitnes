---
phase: 01-foundation-and-auth
plan: 04
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - vitest.config.ts
  - tests/setup.ts
  - tests/auth/auth-config.test.ts
  - tests/auth/signup-login.test.ts
  - tests/auth/session-routing.test.ts
  - tests/ui/landing-page.test.tsx
  - tests/ui/onboarding-shell.test.tsx
  - tests/ui/responsive-shell.test.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-04
  - UX-02
user_setup: []
must_haves:
  truths:
    - Phase 1 execution has committed automated verification for auth configuration, credential flows, session routing, and the initial UI shell.
    - The phase can prove key auth and MVP shell behaviors with named test suites instead of relying only on manual checks.
    - The validation strategy and downstream verify commands reference real files that can be executed during the phase.
  artifacts:
    - vitest.config.ts
    - tests/setup.ts
    - tests/auth/auth-config.test.ts
    - tests/auth/signup-login.test.ts
    - tests/auth/session-routing.test.ts
    - tests/ui/landing-page.test.tsx
    - tests/ui/onboarding-shell.test.tsx
    - tests/ui/responsive-shell.test.tsx
  key_links:
    - 01-02 and 01-03 verification commands reference the test files created here.
    - vitest.config.ts and tests/setup.ts provide the shared execution context for all phase test suites.
---

<objective>
Create the automated validation baseline required for Phase 1 execution.

Purpose: Make the Nyquist validation contract real by giving later plans concrete test files and runnable commands.
Output: Vitest configuration, shared test setup, and initial auth/UI test suites referenced by the rest of the phase plans.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-auth/01-CONTEXT.md
@.planning/phases/01-foundation-and-auth/01-RESEARCH.md
@.planning/phases/01-foundation-and-auth/01-UI-SPEC.md
@.planning/phases/01-foundation-and-auth/01-VALIDATION.md
@.planning/phases/01-foundation-and-auth/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Vitest baseline for the App Router project</name>
  <files>vitest.config.ts, tests/setup.ts</files>
  <read_first>vitest.config.ts, tests/setup.ts, package.json, app/layout.tsx, .planning/phases/01-foundation-and-auth/01-VALIDATION.md</read_first>
  <action>Create `vitest.config.ts` for the Next.js App Router project with jsdom where UI tests need it, shared alias support for `@/`, and test setup loaded from `tests/setup.ts`. Create `tests/setup.ts` with shared test utilities, any global mocks needed for Next.js routing/auth boundaries, and a stable place for future Phase 1 tests to register common helpers. Keep the setup minimal and directly aligned with the auth and UI routes planned in this phase.</action>
  <verify>test -f vitest.config.ts && test -f tests/setup.ts</verify>
  <acceptance_criteria>
    - `vitest.config.ts` exists and contains `defineConfig`.
    - `vitest.config.ts` contains an alias for `@/` or equivalent project-root import support.
    - `tests/setup.ts` exists and is referenced from `vitest.config.ts`.
  </acceptance_criteria>
  <done>The project has a working Vitest baseline for Phase 1 automation.</done>
</task>

<task type="auto">
  <name>Task 2: Add auth-focused test suites referenced by the phase plans</name>
  <files>tests/auth/auth-config.test.ts, tests/auth/signup-login.test.ts, tests/auth/session-routing.test.ts</files>
  <read_first>tests/auth/auth-config.test.ts, tests/auth/signup-login.test.ts, tests/auth/session-routing.test.ts, .planning/phases/01-foundation-and-auth/01-02-PLAN.md, .planning/phases/01-foundation-and-auth/01-VALIDATION.md</read_first>
  <action>Create `tests/auth/auth-config.test.ts` to verify the auth config exports, handler shape, and session settings contract expected by Plan 01-02. Create `tests/auth/signup-login.test.ts` to verify the credential-flow contract, including separate signup/login behavior and redirect expectations. Create `tests/auth/session-routing.test.ts` to verify route protection and incomplete-setup redirect expectations for `/login` and `/onboarding`. These tests can begin as scaffolded expectations and targeted smoke coverage, but they must be real files with runnable Vitest test blocks that later implementation can satisfy.</action>
  <verify>test -f tests/auth/auth-config.test.ts && test -f tests/auth/signup-login.test.ts && test -f tests/auth/session-routing.test.ts</verify>
  <acceptance_criteria>
    - `tests/auth/auth-config.test.ts` exists and contains `describe(`.
    - `tests/auth/signup-login.test.ts` exists and contains `describe(`.
    - `tests/auth/session-routing.test.ts` exists and contains `/onboarding` or `/login`.
  </acceptance_criteria>
  <done>The phase has named, runnable auth test suites that later plans can target directly.</done>
</task>

<task type="auto">
  <name>Task 3: Add UI-shell test suites referenced by the phase plans</name>
  <files>tests/ui/landing-page.test.tsx, tests/ui/onboarding-shell.test.tsx, tests/ui/responsive-shell.test.tsx</files>
  <read_first>tests/ui/landing-page.test.tsx, tests/ui/onboarding-shell.test.tsx, tests/ui/responsive-shell.test.tsx, .planning/phases/01-foundation-and-auth/01-03-PLAN.md, .planning/phases/01-foundation-and-auth/01-UI-SPEC.md</read_first>
  <action>Create `tests/ui/landing-page.test.tsx` to verify the landing-page CTA and headline contract. Create `tests/ui/onboarding-shell.test.tsx` to verify the onboarding placeholder and top-nav shell contract. Create `tests/ui/responsive-shell.test.tsx` to verify the single-column/mobile shell assumptions and approved copy touchpoints. These tests should be real Vitest/Testing Library files with named test cases that later implementation can make pass.</action>
  <verify>test -f tests/ui/landing-page.test.tsx && test -f tests/ui/onboarding-shell.test.tsx && test -f tests/ui/responsive-shell.test.tsx</verify>
  <acceptance_criteria>
    - `tests/ui/landing-page.test.tsx` exists and contains `Create account`.
    - `tests/ui/onboarding-shell.test.tsx` exists and contains `describe(`.
    - `tests/ui/responsive-shell.test.tsx` exists and contains `describe(`.
  </acceptance_criteria>
  <done>The phase has concrete UI-shell test suites wired into the validation strategy.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm vitest run --passWithNoTests` exits 0 or fails only on not-yet-implemented assertions tied to planned behavior
- [ ] All test files referenced by Phase 1 plans exist
- [ ] `01-VALIDATION.md` and Phase 1 plan verify commands reference the same test file names
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 has a concrete automated validation baseline rather than placeholder Nyquist claims
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-auth/01-04-SUMMARY.md`
</output>
