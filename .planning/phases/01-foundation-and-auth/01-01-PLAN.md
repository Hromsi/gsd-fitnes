---
phase: 01-foundation-and-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - components.json
  - .gitignore
  - .env.example
  - app/layout.tsx
  - app/globals.css
  - lib/prisma.ts
  - prisma/schema.prisma
  - prisma/migrations/0001_init/migration.sql
  - prisma/seed.ts
  - scripts/setup-dev.sh
autonomous: true
requirements:
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - The repository can boot as a valid Next.js application with the agreed stack and a documented local setup path.
    - The data layer includes the persistent-session and single-profile schema baseline needed for authenticated sessions and later onboarding.
    - Future auth and UI work can reuse a stable app shell, Prisma client, and seeded local development environment without redoing foundation work.
  artifacts:
    - package.json
    - app/layout.tsx
    - app/globals.css
    - prisma/schema.prisma
    - prisma/seed.ts
    - lib/prisma.ts
    - .env.example
  key_links:
    - prisma/schema.prisma and prisma/seed.ts use the same User/Profile model names and fields.
    - lib/prisma.ts exports a reusable Prisma client used by later auth and data-access code.
---

<objective>
Create the application scaffold and data foundation for the workout planner MVP.

Purpose: Give the remaining Phase 1 plans a stable app, styling, database, and seed baseline.
Output: A runnable Next.js app scaffold with Tailwind, Prisma/PostgreSQL wiring, shadcn initialization metadata, and the initial schema/dev setup files.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-auth/01-CONTEXT.md
@.planning/phases/01-foundation-and-auth/01-RESEARCH.md
@.planning/phases/01-foundation-and-auth/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Next.js app foundation and design-system baseline</name>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, components.json, app/layout.tsx, app/globals.css, .gitignore, .env.example</files>
  <read_first>package.json, app/layout.tsx, app/globals.css, .planning/phases/01-foundation-and-auth/01-CONTEXT.md, .planning/phases/01-foundation-and-auth/01-UI-SPEC.md</read_first>
  <action>Create the initial Next.js App Router project files for a TypeScript + Tailwind app. Configure `package.json` scripts for `dev`, `build`, `start`, `lint`, `prisma:generate`, `prisma:migrate`, and `db:seed`. Add `components.json` configured for `shadcn` with the `new-york` style, `tailwind.css` path `app/globals.css`, `baseColor` `neutral`, and alias mappings rooted at `@/`. Update `app/layout.tsx` to load the Geist font and set the base layout metadata for `Personalized Workout Planner`. Define CSS variables and global styles in `app/globals.css` using the UI-SPEC tokens: dominant `#F7F4EE`, secondary `#E6DED0`, accent `#2F6B57`, destructive `#B84A3A`, and spacing/type rules that preserve the MVP auth-shell direction. Add `.env.example` with `DATABASE_URL=`, `AUTH_SECRET=`, and `AUTH_URL=` placeholders. Ensure `.gitignore` covers `.env`, `.next`, and generated Prisma artifacts that should stay ignored.</action>
  <verify>test -f package.json && test -f app/layout.tsx && test -f components.json && test -f .env.example</verify>
  <acceptance_criteria>
    - `package.json` contains `"dev"`, `"build"`, `"lint"`, `"prisma:generate"`, and `"db:seed"` scripts.
    - `components.json` contains `"style": "new-york"` and `"tailwind": { "css": "app/globals.css"`.
    - `app/layout.tsx` contains `Geist` and `Personalized Workout Planner`.
    - `app/globals.css` contains `#F7F4EE`, `#E6DED0`, `#2F6B57`, and `#B84A3A`.
    - `.env.example` contains `DATABASE_URL=`, `AUTH_SECRET=`, and `AUTH_URL=`.
  </acceptance_criteria>
  <done>All scaffold and design-system baseline files exist with the Phase 1 stack, palette, and environment contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add Prisma client wiring, initial migration, and the auth/profile schema</name>
  <files>prisma/schema.prisma, prisma/migrations/0001_init/migration.sql, prisma/seed.ts, lib/prisma.ts</files>
  <read_first>prisma/schema.prisma, prisma/migrations/0001_init/migration.sql, prisma/seed.ts, lib/prisma.ts, .planning/phases/01-foundation-and-auth/01-CONTEXT.md, .planning/phases/01-foundation-and-auth/01-RESEARCH.md</read_first>
  <action>Define `prisma/schema.prisma` for PostgreSQL with models needed for library-backed auth and the MVP placeholder profile. Include `User` with `id`, `email`, `passwordHash`, timestamps, relation fields for sessions/accounts if the chosen auth library requires them, and an optional `profile Profile?`. Do not add any `role` field, role model, or role-based branching in this phase; every account is a normal user per D-16. Include `Profile` with `id`, `userId @unique`, relation to `User`, and timestamps; keep profile fields minimal so Phase 2 can expand them without breaking one-profile-per-user. If the auth library requires `Session`, `Account`, or `VerificationToken` models, include them in schema form compatible with Prisma and the chosen adapter, while keeping email verification and OAuth flows unused in this phase. Generate and commit the initial migration as `prisma/migrations/0001_init/migration.sql` so the setup workflow has a concrete baseline rather than an implied future step. Add `lib/prisma.ts` exporting a singleton Prisma client using the standard global caching pattern for Next.js development. Add `prisma/seed.ts` that creates a deterministic dev user with a hashed password and an optional empty profile record linked to that user.</action>
  <verify>test -f prisma/schema.prisma && test -f prisma/migrations/0001_init/migration.sql && test -f prisma/seed.ts && test -f lib/prisma.ts</verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains `model User`, `email String @unique`, and `passwordHash`.
    - `prisma/schema.prisma` contains `model Profile` and `userId` with `@unique`.
    - `prisma/schema.prisma` contains `model Session` or the auth-library-equivalent persistent-session model.
    - `prisma/schema.prisma` contains `profile Profile?` or equivalent optional one-to-one relation on `User`.
    - `prisma/schema.prisma` does not contain a `role` field or role model.
    - `prisma/migrations/0001_init/migration.sql` exists.
    - `lib/prisma.ts` contains `new PrismaClient(` and exports a reusable `prisma` client.
    - `prisma/seed.ts` contains a deterministic seed email and creates both a `User` and related `Profile`.
  </acceptance_criteria>
  <done>The database schema, initial migration, and seed path support persistent auth and a single-profile-per-user foundation.</done>
</task>

<task type="auto">
  <name>Task 3: Document and script the local developer setup path</name>
  <files>scripts/setup-dev.sh, package.json, .env.example, prisma/seed.ts</files>
  <read_first>scripts/setup-dev.sh, package.json, .env.example, prisma/seed.ts, .planning/phases/01-foundation-and-auth/01-RESEARCH.md</read_first>
  <action>Create a simple setup helper at `scripts/setup-dev.sh` that runs dependency install guidance, `prisma generate`, and prints the expected bootstrap sequence: copy `.env.example`, run migrations, run seeds, then start dev. Keep it non-destructive and safe for local use. If a README does not yet exist, keep the setup instructions encoded in the script comments and package scripts rather than creating broad project docs outside this plan. Make sure the package scripts and seed script names match exactly so later plans can invoke them without guessing.</action>
  <verify>test -f scripts/setup-dev.sh && rg -n "prisma generate|db:seed|DATABASE_URL" scripts/setup-dev.sh package.json .env.example</verify>
  <acceptance_criteria>
    - `scripts/setup-dev.sh` contains `prisma generate` and `db:seed` or `prisma db seed`.
    - `scripts/setup-dev.sh` contains `.env.example` and `DATABASE_URL`.
    - `package.json` script names referenced by `scripts/setup-dev.sh` exist exactly in `package.json`.
  </acceptance_criteria>
  <done>The repository has a deterministic local setup path that future plans can rely on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` or the package manager equivalent exits 0 after scaffold files are added
- [ ] `pnpm prisma:generate` or equivalent exits 0
- [ ] `prisma/schema.prisma` matches the single-user + single-profile MVP foundation
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository has a runnable scaffold, Prisma client, and initial schema/seed baseline
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-auth/01-01-SUMMARY.md`
</output>
