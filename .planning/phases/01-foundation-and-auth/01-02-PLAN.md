---
phase: 01-foundation-and-auth
plan: 02
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-04"
files_modified:
  - lib/auth.ts
  - app/api/auth/[...nextauth]/route.ts
  - app/(public)/login/page.tsx
  - app/(public)/signup/page.tsx
  - app/(public)/actions/auth.ts
  - app/onboarding/page.tsx
  - middleware.ts
  - lib/current-user.ts
  - lib/validators/auth.ts
  - app/(app)/layout.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - UX-02
user_setup: []
must_haves:
  truths:
    - Users can create accounts and sign in with email and password from separate pages.
    - Sessions persist across refresh and protected routes redirect unauthenticated users to `/login`.
    - Server-side user resolution and route protection are scoped to the signed-in user, with concurrent sessions allowed and no role-based branching introduced.
  artifacts:
    - lib/auth.ts
    - app/api/auth/[...nextauth]/route.ts
    - app/(public)/login/page.tsx
    - app/(public)/signup/page.tsx
    - app/onboarding/page.tsx
    - middleware.ts
    - lib/current-user.ts
  key_links:
    - signup action creates `User` records compatible with the auth configuration in `lib/auth.ts`.
    - middleware.ts, app/(app)/layout.tsx, and app/onboarding/page.tsx enforce the same authenticated onboarding-first route model.
---

<objective>
Implement the actual authentication and session behavior for the workout planner MVP.

Purpose: Deliver the core auth requirements so users can sign up, sign in, sign out, and stay authenticated across refreshes.
Output: Auth library configuration, credential-based auth flows, protected-route handling, and user-scoped server utilities.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-auth/01-CONTEXT.md
@.planning/phases/01-foundation-and-auth/01-RESEARCH.md
@.planning/phases/01-foundation-and-auth/01-UI-SPEC.md
@.planning/phases/01-foundation-and-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-auth/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure library-backed auth with credentials and persistent sessions</name>
  <files>lib/auth.ts, app/api/auth/[...nextauth]/route.ts, middleware.ts, lib/current-user.ts</files>
  <read_first>lib/auth.ts, app/api/auth/[...nextauth]/route.ts, middleware.ts, lib/current-user.ts, prisma/schema.prisma, lib/prisma.ts, .planning/phases/01-foundation-and-auth/01-CONTEXT.md, .planning/phases/01-foundation-and-auth/01-RESEARCH.md</read_first>
  <action>Implement the chosen auth library configuration in `lib/auth.ts` using a Prisma-backed adapter and credentials provider for email/password login. Configure explicit persistent-session behavior to match D-05: use database sessions, set `maxAge: 31536000` seconds (365 days), refresh the active session on authenticated activity, and make explicit sign-out the only product-level way to end a still-active session. Preserve D-07 by allowing multiple concurrent sessions for the same account and by avoiding any device/session-management UI, single-session invalidation logic, or forced revocation behavior in Phase 1. Export auth helpers used by route handlers and server components. Add `app/api/auth/[...nextauth]/route.ts` wiring to expose GET/POST handlers. Add `middleware.ts` that redirects unauthenticated requests for protected routes to `/login`, while allowing public routes such as `/`, `/login`, `/signup`, and static assets. In the same middleware flow, add an explicit incomplete-setup redirect so authenticated users whose setup state is incomplete are routed to `/onboarding` whenever they request other protected destinations. Create `lib/current-user.ts` that resolves the active session and returns the current user record from the database for secure server-side ownership checks.</action>
  <verify>test -f lib/auth.ts && test -f app/api/auth/[...nextauth]/route.ts && test -f middleware.ts && test -f lib/current-user.ts && pnpm vitest run tests/auth/auth-config.test.ts</verify>
  <acceptance_criteria>
    - `lib/auth.ts` contains a credentials provider and Prisma adapter usage or equivalent library-backed session setup.
    - `lib/auth.ts` contains `maxAge: 31536000` or an equivalent explicit one-year persistent session setting.
    - `lib/auth.ts` or related auth code contains no single-session invalidation or device-management logic.
    - `app/api/auth/[...nextauth]/route.ts` exports `GET` and `POST`.
    - `middleware.ts` contains `/login` and protected-route redirect logic.
    - `middleware.ts` contains `/onboarding` redirect logic for authenticated users with incomplete setup.
    - `lib/current-user.ts` contains a function that resolves the current session user from Prisma by `id` or `email`.
  </acceptance_criteria>
  <done>Auth configuration, session handling, and protected-route gating are present and connected.</done>
</task>

<task type="auto">
  <name>Task 2: Build separate login and signup flows with secure credential handling</name>
  <files>app/(public)/login/page.tsx, app/(public)/signup/page.tsx, app/(public)/actions/auth.ts, app/onboarding/page.tsx, lib/validators/auth.ts</files>
  <read_first>app/(public)/login/page.tsx, app/(public)/signup/page.tsx, app/(public)/actions/auth.ts, app/onboarding/page.tsx, lib/validators/auth.ts, .planning/phases/01-foundation-and-auth/01-CONTEXT.md, .planning/phases/01-foundation-and-auth/01-UI-SPEC.md</read_first>
  <action>Create separate login and signup pages that follow the UI-SPEC: centered auth card on desktop, single-column form-first layout on mobile, form title as the focal point, and the submit button as the secondary visual draw. Add a shared validation module at `lib/validators/auth.ts` for exact fields `email` and `password`. Implement `app/(public)/actions/auth.ts` with a signup action that hashes passwords before inserting a `User`, creates a placeholder `Profile` when appropriate, marks setup as incomplete, signs the user in after successful signup, and redirects directly to `/onboarding`. Create `app/onboarding/page.tsx` as a minimal protected onboarding placeholder so the redirect target exists in this same plan and clearly signals that workout setup continues here next. Implement login submission that authenticates with the credentials provider and shows inline field-level or form-level errors using the approved copy style. Do not add email verification, forgot-password, remember-me, or social login controls.</action>
  <verify>test -f "app/(public)/login/page.tsx" && test -f "app/(public)/signup/page.tsx" && test -f "app/(public)/actions/auth.ts" && test -f "app/onboarding/page.tsx" && pnpm vitest run tests/auth/signup-login.test.ts</verify>
  <acceptance_criteria>
    - `app/(public)/login/page.tsx` contains `Log in` and an email/password form.
    - `app/(public)/signup/page.tsx` contains `Create account` and an email/password form.
    - `app/(public)/actions/auth.ts` contains a password hashing call and a redirect to `/onboarding`.
    - `app/onboarding/page.tsx` exists and contains setup-oriented placeholder copy.
    - `lib/validators/auth.ts` contains validation for `email` and `password`.
    - Login and signup files do not contain `Remember me`, `Forgot password`, or OAuth provider buttons.
  </acceptance_criteria>
  <done>Users can create accounts and sign in through separate, MVP-scoped credential forms with a real onboarding redirect target in place.</done>
</task>

<task type="auto">
  <name>Task 3: Establish server-side ownership helpers and sign-out support</name>
  <files>lib/current-user.ts, lib/auth.ts, app/(public)/actions/auth.ts, app/(app)/layout.tsx</files>
  <read_first>lib/current-user.ts, lib/auth.ts, app/(public)/actions/auth.ts, app/(app)/layout.tsx, prisma/schema.prisma, .planning/phases/01-foundation-and-auth/01-CONTEXT.md</read_first>
  <action>Add a current-user/ownership helper surface that later phases can use for all user-scoped reads and writes. Create or update `app/(app)/layout.tsx` so protected app pages require an authenticated session, can surface minimal signed-in navigation data, and redirect authenticated users with incomplete setup to `/onboarding` when they attempt to access other protected app routes. Add sign-out support using the auth library's standard sign-out flow and make sure the destructive confirmation copy from the UI-SPEC can be used by the shell in Plan 03. Keep the ownership model explicit: all protected access should resolve the current user first and never assume a client-passed user id.</action>
  <verify>test -f "app/(app)/layout.tsx" && rg -n "signOut|logout|current user|currentUser" lib/auth.ts lib/current-user.ts "app/(app)/layout.tsx" "app/(public)/actions/auth.ts" && pnpm vitest run tests/auth/session-routing.test.ts</verify>
  <acceptance_criteria>
    - `app/(app)/layout.tsx` exists and contains an auth/session gate or current-user lookup.
    - `app/(app)/layout.tsx` contains `/onboarding` redirect logic or an equivalent incomplete-setup gate.
    - `lib/current-user.ts` exposes a reusable current-user helper.
    - At least one file contains `signOut` or equivalent logout flow wiring.
    - No server helper accepts a raw client-supplied `userId` as the source of authorization truth.
  </acceptance_criteria>
  <done>The project has a reusable, secure foundation for user-scoped server access and sign-out behavior, with concurrent sessions allowed and no role-based auth branching.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` or equivalent exits 0
- [ ] Auth configuration loads without TypeScript errors
- [ ] Manual auth smoke test proves signup, login, refresh persistence, and sign-out work
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can sign up, sign in, sign out, and stay authenticated across refreshes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-auth/01-02-SUMMARY.md`
</output>
