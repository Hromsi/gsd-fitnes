---
phase: 03-deterministic-plan-generator
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/0004_generated_plan_snapshots/migration.sql
  - app/(app)/actions/generate-plan.ts
  - app/(app)/next-step/page.tsx
  - lib/workouts/save-generated-plan.ts
  - tests/generator/plan-persistence.test.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-04
must_haves:
  truths:
    - Generated plans are persisted as user-owned snapshots rather than dynamic views over the current exercise library.
    - The domain model supports preserving older plans while still identifying the current active one.
    - First-plan generation is explicit and user-triggered from the existing post-onboarding flow, without adding regenerate UI yet.
  artifacts:
    - prisma/schema.prisma
    - prisma/migrations/0004_generated_plan_snapshots/migration.sql
    - app/(app)/actions/generate-plan.ts
    - app/(app)/next-step/page.tsx
    - lib/workouts/save-generated-plan.ts
    - tests/generator/plan-persistence.test.ts
  key_links:
    - The generation action reads the current user profile, uses the 03-02 generator service, and writes snapshot data via the persistence helper.
    - The next-step page becomes the minimal first-generation UI bridge defined by the Phase 3 UI contract.
---

<objective>
Persist generated plan snapshots and connect first-plan generation to the post-onboarding flow.

Purpose: Make generation real by storing output and exposing the first explicit generation action.
Output: Snapshot persistence model, save helper, first-plan generation action, and a minimal generation-ready UI bridge.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deterministic-plan-generator/03-CONTEXT.md
@.planning/phases/03-deterministic-plan-generator/03-RESEARCH.md
@.planning/phases/03-deterministic-plan-generator/03-UI-SPEC.md
@.planning/phases/03-deterministic-plan-generator/03-01-SUMMARY.md
@.planning/phases/03-deterministic-plan-generator/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add snapshot persistence models for plans, workouts, and workout exercises</name>
  <files>prisma/schema.prisma, prisma/migrations/0004_generated_plan_snapshots/migration.sql, lib/workouts/save-generated-plan.ts</files>
  <read_first>prisma/schema.prisma, lib/workouts/save-generated-plan.ts, .planning/phases/03-deterministic-plan-generator/03-RESEARCH.md</read_first>
  <action>Extend the Prisma schema with the minimum snapshot persistence models required for generated plans. Include an owning plan model tied to the user, child workout-day rows representing the 7-day structure, and child workout-exercise rows that preserve ordering and the chosen exercise reference. Ensure the schema can distinguish the current active plan while still allowing older generated plans to remain stored. Add `prisma/migrations/0004_generated_plan_snapshots/migration.sql` and create `lib/workouts/save-generated-plan.ts` as the reusable persistence helper that writes a generated plan snapshot transactionally.</action>
  <verify>test -f prisma/migrations/0004_generated_plan_snapshots/migration.sql && test -f lib/workouts/save-generated-plan.ts && rg -n "active|WorkoutDay|WorkoutExercise|GeneratedPlan|Plan" prisma/schema.prisma</verify>
  <acceptance_criteria>
    - Snapshot persistence models exist for plan, workout day, and workout exercise layers.
    - The schema supports one active current plan while preserving older plans.
    - Persistence logic is encapsulated in a reusable helper.
  </acceptance_criteria>
  <done>The codebase can store generated plan snapshots durably and user-safely.</done>
</task>

<task type="auto">
  <name>Task 2: Add first-plan generation action and connect it to the next-step screen</name>
  <files>app/(app)/actions/generate-plan.ts, app/(app)/next-step/page.tsx</files>
  <read_first>app/(app)/next-step/page.tsx, app/(app)/actions/generate-plan.ts, lib/current-user.ts, lib/workouts/generate-plan.ts, lib/workouts/save-generated-plan.ts</read_first>
  <action>Create a server action such as `app/(app)/actions/generate-plan.ts` that resolves the current signed-in user, reads the saved profile, invokes the generator service, persists the result as a snapshot, and returns a success state for the UI. Update `app/(app)/next-step/page.tsx` so it becomes the minimal generation-ready surface approved in the UI-SPEC: practical copy, one primary `Generate workout plan` action, and clear loading text. Do not add regenerate UI or the final weekly schedule view yet; keep the page focused on first-plan creation only.</action>
  <verify>test -f app/(app)/actions/generate-plan.ts && rg -n "Generate workout plan|Generating plan" app/(app)/next-step/page.tsx app/(app)/actions/generate-plan.ts && pnpm vitest run tests/generator/plan-persistence.test.ts</verify>
  <acceptance_criteria>
    - There is a single explicit first-plan generation action.
    - The next-step screen contains the approved primary CTA and loading-state copy.
    - Generation is user-triggered, not automatic after onboarding.
    - No regenerate button is introduced yet.
  </acceptance_criteria>
  <done>Users have a minimal, explicit bridge from profile completion into first-plan generation.</done>
</task>

<task type="auto">
  <name>Task 3: Add persistence/integration tests for generated plan snapshots</name>
  <files>tests/generator/plan-persistence.test.ts</files>
  <read_first>tests/generator/plan-persistence.test.ts, app/(app)/actions/generate-plan.ts, lib/workouts/save-generated-plan.ts</read_first>
  <action>Create `tests/generator/plan-persistence.test.ts` to verify the snapshot contract: a generated plan can be transformed into persistence rows, active/current semantics are represented, and the generator action remains explicit rather than auto-triggered by profile changes. The tests can mock Prisma-level writes if needed, but they should prove that 03-03 really bridges generation output into snapshot persistence for later phases.</action>
  <verify>test -f tests/generator/plan-persistence.test.ts && pnpm vitest run tests/generator/plan-persistence.test.ts</verify>
  <acceptance_criteria>
    - The plan-persistence test suite exists and runs.
    - The suite verifies active/current plan semantics and snapshot persistence structure.
    - Tests confirm generation is explicit and user-triggered.
  </acceptance_criteria>
  <done>The Phase 3 handoff from profile completion to persisted plan snapshots is verified and ready for Phase 4.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm vitest run tests/generator/plan-persistence.test.ts` exits 0
- [ ] `pnpm build` exits 0
- [ ] Manual check confirms the next-step page exposes only one primary first-generation action
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app can generate and persist a first user-owned workout plan snapshot through an explicit action
</success_criteria>

<output>
After completion, create `.planning/phases/03-deterministic-plan-generator/03-03-SUMMARY.md`
</output>
