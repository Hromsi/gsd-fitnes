---
phase: 03-deterministic-plan-generator
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - lib/workouts/templates.ts
  - lib/workouts/generate-plan.ts
  - lib/workouts/types.ts
  - tests/generator/plan-templates.test.ts
  - tests/generator/plan-generation.test.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-04
must_haves:
  truths:
    - The generator uses fixed weekly templates as its backbone.
    - Profile inputs influence output through the locked MVP rules: frequency selects training-day count, level shapes exercise difficulty, goal modestly adjusts volume, and equipment narrows practical exercise choices.
    - Generation logic lives in reusable domain/service code rather than route-level ad hoc assembly.
  artifacts:
    - lib/workouts/templates.ts
    - lib/workouts/generate-plan.ts
    - tests/generator/plan-templates.test.ts
    - tests/generator/plan-generation.test.ts
  key_links:
    - The generation service consumes the exercise library from 03-01 and produces snapshot-ready structures for 03-03.
    - Weekly template definitions and the generator tests encode the same approved rule set.
---

<objective>
Implement the deterministic workout-plan generation rules and template system.

Purpose: Translate the saved profile into a structured 7-day plan domain object.
Output: Weekly templates, generation service, and tests that prove the generator respects the MVP rule set.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deterministic-plan-generator/03-CONTEXT.md
@.planning/phases/03-deterministic-plan-generator/03-RESEARCH.md
@.planning/phases/03-deterministic-plan-generator/03-UI-SPEC.md
@.planning/phases/03-deterministic-plan-generator/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define workout-plan domain types and fixed weekly templates</name>
  <files>lib/workouts/types.ts, lib/workouts/templates.ts, tests/generator/plan-templates.test.ts</files>
  <read_first>lib/workouts/types.ts, lib/workouts/templates.ts, .planning/phases/03-deterministic-plan-generator/03-CONTEXT.md</read_first>
  <action>Create plan-domain types that represent a generated 7-day plan, workout days, rest days, and workout exercises in a snapshot-ready way. Add `lib/workouts/templates.ts` with fixed weekly templates keyed by or derived from the allowed training frequencies, explicitly representing both workout days and rest days. Keep the templates small and readable. Add `tests/generator/plan-templates.test.ts` to verify that the templates always span 7 days and preserve explicit rest-day representation.</action>
  <verify>test -f lib/workouts/templates.ts && test -f lib/workouts/types.ts && pnpm vitest run tests/generator/plan-templates.test.ts</verify>
  <acceptance_criteria>
    - Weekly templates cover a full 7-day cycle.
    - Rest days are represented explicitly.
    - Domain types are reusable by the persistence layer in 03-03.
  </acceptance_criteria>
  <done>The project has explicit template and type foundations for deterministic generation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the generator service using the approved rule set</name>
  <files>lib/workouts/generate-plan.ts, lib/workouts/templates.ts, lib/workouts/exercise-library.ts</files>
  <read_first>lib/workouts/generate-plan.ts, lib/workouts/templates.ts, lib/workouts/exercise-library.ts, .planning/phases/03-deterministic-plan-generator/03-RESEARCH.md</read_first>
  <action>Build `lib/workouts/generate-plan.ts` as the core domain service that accepts the saved profile inputs and returns a generated 7-day plan structure. The implementation should follow the locked behavior exactly: fixed templates as the base, frequency selects the weekly structure, level influences difficulty selection, goal adjusts volume modestly, and equipment influences exercise selection practically but not through a needlessly rigid matrix. Allow light bounded variability where appropriate, but keep the output structured and predictable enough to test. Make sure generated workouts include day label, workout type, exercise list, and short sets/reps or duration guidance, while each generated exercise entry stores just an exercise reference plus order in the returned structure.</action>
  <verify>test -f lib/workouts/generate-plan.ts && pnpm vitest run tests/generator/plan-generation.test.ts</verify>
  <acceptance_criteria>
    - The generator service accepts the four profile inputs.
    - Training frequency changes the number of workout days.
    - Goal changes volume modestly rather than selecting entirely different weekly architectures.
    - Level influences exercise difficulty selection.
    - Equipment influences selection without requiring replacement chains.
  </acceptance_criteria>
  <done>The core deterministic generator exists as reusable business logic.</done>
</task>

<task type="auto">
  <name>Task 3: Add generator verification for structure and profile-dependent output</name>
  <files>tests/generator/plan-generation.test.ts</files>
  <read_first>tests/generator/plan-generation.test.ts, lib/workouts/generate-plan.ts, lib/workouts/templates.ts</read_first>
  <action>Create `tests/generator/plan-generation.test.ts` to verify key generation guarantees: the output spans seven days, workout/rest day counts follow frequency templates, category/equipment/level rules affect selection, and the generated workout shape includes day metadata plus short guidance. Since light variability is allowed, write assertions against structural and rule-based invariants rather than one exact plan snapshot unless a specific case is intentionally fixed.</action>
  <verify>test -f tests/generator/plan-generation.test.ts && pnpm vitest run tests/generator/plan-generation.test.ts</verify>
  <acceptance_criteria>
    - The generator test suite exists and runs.
    - Tests assert rule compliance rather than brittle exact full-plan snapshots where variability exists.
    - The suite proves output structure and profile-dependent differences.
  </acceptance_criteria>
  <done>The generator logic is protected by meaningful domain tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm vitest run tests/generator/plan-templates.test.ts tests/generator/plan-generation.test.ts` exits 0
- [ ] `pnpm lint` exits 0
- [ ] The generator output shape is ready to persist as snapshots
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The codebase can generate a rule-based 7-day plan from the saved profile using fixed templates and library data
</success_criteria>

<output>
After completion, create `.planning/phases/03-deterministic-plan-generator/03-02-SUMMARY.md`
</output>
