---
phase: 03-deterministic-plan-generator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/0003_exercise_library/migration.sql
  - prisma/seed.ts
  - lib/validators/exercise.ts
  - lib/workouts/exercise-library.ts
  - tests/generator/exercise-library.test.ts
autonomous: true
requirements:
  - PLAN-04
must_haves:
  truths:
    - The project has a small, controlled internal exercise library suitable for MVP plan generation.
    - Exercise records store the approved metadata only: name, description, equipment, level, and category.
    - Exercise content is seeded deterministically and can be reused by the generator without UI coupling.
  artifacts:
    - prisma/schema.prisma
    - prisma/migrations/0003_exercise_library/migration.sql
    - prisma/seed.ts
    - lib/workouts/exercise-library.ts
    - tests/generator/exercise-library.test.ts
  key_links:
    - Prisma schema and seed data define the same exercise-library fields and allowed values.
    - The library helper exposes the same exercise categories/equipment semantics the generator will consume in 03-02.
---

<objective>
Model and seed the exercise library required by the deterministic generator.

Purpose: Establish the reusable workout content foundation for plan generation.
Output: Exercise library schema, migration, seed data, and helper utilities for querying/organizing exercises by the approved metadata.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deterministic-plan-generator/03-CONTEXT.md
@.planning/phases/03-deterministic-plan-generator/03-RESEARCH.md
@.planning/phases/03-deterministic-plan-generator/03-UI-SPEC.md
@.planning/phases/02-fitness-profile-onboarding/02-01-SUMMARY.md
@.planning/phases/02-fitness-profile-onboarding/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exercise-library schema and migration</name>
  <files>prisma/schema.prisma, prisma/migrations/0003_exercise_library/migration.sql</files>
  <read_first>prisma/schema.prisma, .planning/phases/03-deterministic-plan-generator/03-CONTEXT.md, .planning/phases/03-deterministic-plan-generator/03-RESEARCH.md</read_first>
  <action>Extend the Prisma schema with the minimum exercise-library model(s) needed for the MVP. Store the approved metadata only: `name`, `description`, `equipment`, `level`, and `category`. Use explicit enums or equally strict constraints for equipment, level, and category so the library stays controlled and testable. Keep the design intentionally small and avoid speculative replacement relationships or admin-oriented content structures. Commit the matching migration as `prisma/migrations/0003_exercise_library/migration.sql`.</action>
  <verify>test -f prisma/migrations/0003_exercise_library/migration.sql && rg -n "model Exercise|description|equipment|level|category" prisma/schema.prisma</verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains an exercise-library model with `name`, `description`, `equipment`, `level`, and `category`.
    - The schema does not introduce replacement/exchange logic.
    - `prisma/migrations/0003_exercise_library/migration.sql` exists.
  </acceptance_criteria>
  <done>The exercise library is represented in the database in an MVP-scoped way.</done>
</task>

<task type="auto">
  <name>Task 2: Seed a small curated library and expose query helpers</name>
  <files>prisma/seed.ts, lib/validators/exercise.ts, lib/workouts/exercise-library.ts</files>
  <read_first>prisma/seed.ts, .planning/phases/03-deterministic-plan-generator/03-CONTEXT.md, .planning/phases/03-deterministic-plan-generator/03-RESEARCH.md</read_first>
  <action>Extend `prisma/seed.ts` with a small, hand-curated starter library that covers the required workout categories while staying intentionally limited. Add `lib/validators/exercise.ts` or a similar shared module to lock the allowed metadata values, and add `lib/workouts/exercise-library.ts` with helper functions that make it easy for the generator to retrieve or group exercises by category, equipment, and level. Keep the seed deterministic and readable rather than content-heavy.</action>
  <verify>rg -n "upper|lower|full_body|core|cardio" prisma/seed.ts lib/validators/exercise.ts lib/workouts/exercise-library.ts && test -f lib/workouts/exercise-library.ts</verify>
  <acceptance_criteria>
    - The seeded library is intentionally small and curated.
    - Shared validation/constants exist for exercise metadata.
    - Library helper code exposes category/equipment/level-aware querying or grouping.
  </acceptance_criteria>
  <done>The project has seeded exercise content and reusable helper access patterns for generation.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated verification for the exercise library</name>
  <files>tests/generator/exercise-library.test.ts</files>
  <read_first>tests/generator/exercise-library.test.ts, lib/workouts/exercise-library.ts, prisma/seed.ts</read_first>
  <action>Create `tests/generator/exercise-library.test.ts` to verify the exercise-library contract: approved metadata fields exist, category coverage matches the MVP workout types, and helper/query functions can separate exercises by equipment and level without needing full generation logic yet. The tests can stay focused on domain contracts, but they should be real Vitest suites that the later generator plan can rely on.</action>
  <verify>test -f tests/generator/exercise-library.test.ts && pnpm vitest run tests/generator/exercise-library.test.ts</verify>
  <acceptance_criteria>
    - `tests/generator/exercise-library.test.ts` exists.
    - The test suite verifies category coverage and controlled metadata assumptions.
    - The tests are runnable and targeted at the library helper surface.
  </acceptance_criteria>
  <done>The exercise-library foundation is test-backed and ready for template-driven generation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm prisma:generate` exits 0
- [ ] `pnpm vitest run tests/generator/exercise-library.test.ts` exits 0
- [ ] `pnpm lint` exits 0
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The codebase has a seeded, queryable internal exercise library ready for generation logic
</success_criteria>

<output>
After completion, create `.planning/phases/03-deterministic-plan-generator/03-01-SUMMARY.md`
</output>
