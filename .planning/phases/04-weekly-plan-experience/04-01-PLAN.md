---
phase: 04-weekly-plan-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/app/page.tsx
  - app/(app)/next-step/page.tsx
  - lib/current-user.ts
  - components/workouts/weekly-plan-view.tsx
  - components/workouts/plan-day-card.tsx
  - tests/workouts/weekly-plan-routing.test.ts
  - tests/workouts/weekly-plan-view.test.tsx
autonomous: true
requirements:
  - WORK-01
must_haves:
  truths:
    - `/app` becomes the main entry point into the active weekly plan experience.
    - Users without an active plan still route to the fallback `next-step` screen.
    - The weekly schedule shows all seven days with visible rest days, today highlight, and concise workout previews.
  artifacts:
    - app/(app)/app/page.tsx
    - components/workouts/weekly-plan-view.tsx
    - components/workouts/plan-day-card.tsx
    - tests/workouts/weekly-plan-routing.test.ts
    - tests/workouts/weekly-plan-view.test.tsx
  key_links:
    - Phase 3 snapshot persistence is the sole source of weekly-plan data.
    - The weekly view contract and routing behavior match the Phase 4 UI-SPEC and context decisions.
---

<objective>
Build the weekly plan page and establish current-plan routing.

Purpose: Convert the authenticated app destination into a real active-plan experience.
Output: Active-plan routing logic, weekly card view, and tests for fallback and rendering behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-weekly-plan-experience/04-CONTEXT.md
@.planning/phases/04-weekly-plan-experience/04-RESEARCH.md
@.planning/phases/04-weekly-plan-experience/04-UI-SPEC.md
@.planning/phases/03-deterministic-plan-generator/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route App through active-plan presence</name>
  <files>app/(app)/app/page.tsx, app/(app)/next-step/page.tsx, lib/current-user.ts</files>
  <read_first>app/(app)/app/page.tsx, app/(app)/next-step/page.tsx, lib/current-user.ts</read_first>
  <action>Replace the current `/app` placeholder with ownership-safe active-plan resolution. If the signed-in user has no active plan, redirect them to `/next-step`. If they do have an active plan, load the data needed for the weekly schedule route. Keep `next-step` as the fallback screen only for the no-plan state.</action>
  <verify>rg -n "redirect\\(\"/next-step\"|workoutPlan" app/'(app)'/app/page.tsx app/'(app)'/next-step/page.tsx lib/current-user.ts</verify>
  <acceptance_criteria>
    - `/app` no longer renders a placeholder empty state.
    - Users without an active plan still reach `next-step`.
    - Users with an active plan are routed into the weekly plan experience.
  </acceptance_criteria>
  <done>The app entry point now reflects real active-plan state.</done>
</task>

<task type="auto">
  <name>Task 2: Build the weekly card-based schedule view</name>
  <files>components/workouts/weekly-plan-view.tsx, components/workouts/plan-day-card.tsx, app/(app)/app/page.tsx</files>
  <read_first>components/workouts/generate-plan-form.tsx, app/(app)/app/page.tsx, .planning/phases/04-weekly-plan-experience/04-UI-SPEC.md</read_first>
  <action>Create weekly-view components that render all seven days from the active plan snapshot as cards. Workout cards should show day label, workout type, and a short exercise preview. Rest days should stay visible with a quieter treatment. Highlight the current day, and keep the layout simple and mobile-safe.</action>
  <verify>test -f components/workouts/weekly-plan-view.tsx && test -f components/workouts/plan-day-card.tsx</verify>
  <acceptance_criteria>
    - Weekly view renders all seven days.
    - Rest days remain visible.
    - Today is highlighted.
    - Workout cards show a short preview of exercises.
  </acceptance_criteria>
  <done>The user has a usable weekly schedule surface for the active plan.</done>
</task>

<task type="auto">
  <name>Task 3: Add weekly view routing and rendering tests</name>
  <files>tests/workouts/weekly-plan-routing.test.ts, tests/workouts/weekly-plan-view.test.tsx</files>
  <read_first>app/(app)/app/page.tsx, components/workouts/weekly-plan-view.tsx</read_first>
  <action>Add tests that verify `/app` correctly falls back to `next-step` when no active plan exists, and that the weekly plan view renders seven days with explicit workout/rest states and current-day emphasis. Keep the tests focused on route contracts and UI structure rather than full E2E detail behavior.</action>
  <verify>pnpm vitest run tests/workouts/weekly-plan-routing.test.ts tests/workouts/weekly-plan-view.test.tsx</verify>
  <acceptance_criteria>
    - Weekly route tests exist and run.
    - The test suite verifies fallback routing and weekly rendering shape.
    - The tests lock the visible rest-day and current-day behavior.
  </acceptance_criteria>
  <done>The Phase 4 weekly-view foundation is test-backed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm vitest run tests/workouts/weekly-plan-routing.test.ts tests/workouts/weekly-plan-view.test.tsx` exits 0
- [ ] `pnpm lint` exits 0
- [ ] `/app` only falls back to `next-step` when the user has no active plan
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app has a real weekly plan entry point instead of a placeholder
</success_criteria>

<output>
After completion, create `.planning/phases/04-weekly-plan-experience/04-01-SUMMARY.md`
</output>
