---
phase: 04-weekly-plan-experience
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - app/(app)/actions/generate-plan.ts
  - app/(app)/settings/profile/page.tsx
  - components/workouts/regenerate-plan-card.tsx
  - app/(app)/app/page.tsx
  - tests/workouts/regenerate-plan-flow.test.tsx
  - tests/workouts/app-entry-routing.test.ts
autonomous: true
requirements:
  - PLAN-03
  - WORK-01
must_haves:
  truths:
    - Manual regenerate is available from profile settings, not from the weekly schedule.
    - Regeneration requires lightweight confirmation and activates the newly generated plan.
    - After regeneration, the user remains in the weekly plan experience with refreshed active data.
  artifacts:
    - app/(app)/settings/profile/page.tsx
    - components/workouts/regenerate-plan-card.tsx
    - app/(app)/actions/generate-plan.ts
    - tests/workouts/regenerate-plan-flow.test.tsx
    - tests/workouts/app-entry-routing.test.ts
  key_links:
    - The regenerate flow reuses the Phase 3 generation and snapshot persistence services.
    - App entry routing after regeneration still respects the active-plan-first rule established in 04-01.
---

<objective>
Add manual regenerate flow and finalize Phase 4 routing behavior.

Purpose: Let the user replace the active plan intentionally without leaving the approved MVP interaction pattern.
Output: Regenerate entry in settings, confirmation UI, refreshed active-plan behavior, and tests for replacement-plan handling.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-weekly-plan-experience/04-CONTEXT.md
@.planning/phases/04-weekly-plan-experience/04-RESEARCH.md
@.planning/phases/04-weekly-plan-experience/04-UI-SPEC.md
@.planning/phases/03-deterministic-plan-generator/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regenerate entry and confirmation in profile settings</name>
  <files>app/(app)/settings/profile/page.tsx, components/workouts/regenerate-plan-card.tsx</files>
  <read_first>app/(app)/settings/profile/page.tsx, app/(app)/actions/generate-plan.ts, .planning/phases/04-weekly-plan-experience/04-UI-SPEC.md</read_first>
  <action>Add a regenerate section to `settings/profile` that sits alongside profile editing without dominating the page. It should require a short explicit confirmation before triggering regeneration. Keep the UX lightweight and aligned with the approved MVP shell.</action>
  <verify>test -f components/workouts/regenerate-plan-card.tsx && rg -n "Regenerate|confirm" app/'(app)'/settings/profile/page.tsx components/workouts/regenerate-plan-card.tsx</verify>
  <acceptance_criteria>
    - Regenerate entry lives in profile settings.
    - Confirmation is required before the action runs.
    - The weekly canvas itself does not gain regenerate controls.
  </acceptance_criteria>
  <done>The user has an intentional and low-risk entry point for replacing the active plan.</done>
</task>

<task type="auto">
  <name>Task 2: Reuse generation action for replacement-plan handling and redirect back to weekly view</name>
  <files>app/(app)/actions/generate-plan.ts, app/(app)/app/page.tsx</files>
  <read_first>app/(app)/actions/generate-plan.ts, lib/workouts/save-generated-plan.ts, app/(app)/app/page.tsx</read_first>
  <action>Extend or adapt the existing generation action so it can serve both first-plan creation and regenerate flows cleanly. Ensure the new plan becomes active, older plans remain stored, and the user ends up back in the weekly plan experience with refreshed content. Keep `/app` as the stable entry point for users with an active plan.</action>
  <verify>rg -n "isActive: false|isActive: true|redirect|refresh" app/'(app)'/actions/generate-plan.ts lib/workouts/save-generated-plan.ts app/'(app)'/app/page.tsx</verify>
  <acceptance_criteria>
    - Regeneration uses the same core generation/persistence path.
    - The newly generated plan becomes active.
    - The user remains in or returns to the weekly plan view after success.
  </acceptance_criteria>
  <done>The regenerate flow produces a clean active-plan replacement path.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for regenerate flow and final app entry routing</name>
  <files>tests/workouts/regenerate-plan-flow.test.tsx, tests/workouts/app-entry-routing.test.ts</files>
  <read_first>app/(app)/settings/profile/page.tsx, app/(app)/actions/generate-plan.ts, app/(app)/app/page.tsx</read_first>
  <action>Add tests that verify regenerate UI lives in settings/profile, confirmation is required, and app entry routing continues to send no-plan users to `next-step` and active-plan users to the weekly schedule. Include assertions that regeneration promotes the new plan to active status without deleting older plan history.</action>
  <verify>pnpm vitest run tests/workouts/regenerate-plan-flow.test.tsx tests/workouts/app-entry-routing.test.ts</verify>
  <acceptance_criteria>
    - Regenerate flow tests exist and run.
    - The suite verifies confirmation and active-plan replacement semantics.
    - The suite locks the final Phase 4 entry-routing contract.
  </acceptance_criteria>
  <done>The Phase 4 handoff into active-plan-first app behavior is test-backed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm vitest run tests/workouts/regenerate-plan-flow.test.tsx tests/workouts/app-entry-routing.test.ts` exits 0
- [ ] `pnpm lint` exits 0
- [ ] Manual check confirms regenerate is only exposed from profile settings
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The user can replace the active plan intentionally and continue through the weekly experience
</success_criteria>

<output>
After completion, create `.planning/phases/04-weekly-plan-experience/04-03-SUMMARY.md`
</output>
