---
phase: 04-weekly-plan-experience
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - app/(app)/app/@modal/(.)day/[dayId]/page.tsx
  - app/(app)/app/day/[dayId]/page.tsx
  - components/workouts/workout-detail-modal.tsx
  - lib/workouts/get-active-plan-day.ts
  - tests/workouts/workout-detail-routing.test.ts
  - tests/workouts/workout-detail-modal.test.tsx
autonomous: true
requirements:
  - WORK-02
  - WORK-03
must_haves:
  truths:
    - Workout detail opens as a route-driven modal over the weekly schedule.
    - Detail content includes guidance, ordered exercises, and descriptions.
    - Rest days also have a simple detail state, while inactive-plan direct access is blocked.
  artifacts:
    - app/(app)/app/@modal/(.)day/[dayId]/page.tsx
    - app/(app)/app/day/[dayId]/page.tsx
    - components/workouts/workout-detail-modal.tsx
    - tests/workouts/workout-detail-routing.test.ts
    - tests/workouts/workout-detail-modal.test.tsx
  key_links:
    - The detail modal reads from the same active plan snapshot rendered in the weekly view.
    - Route-level ownership and active-plan checks enforce the MVP rule that inactive history is not directly browseable.
---

<objective>
Build the route-driven workout detail modal.

Purpose: Let the user inspect one workout day without leaving the weekly-plan context.
Output: Detail routes, modal component, active-day loader, and tests for workout/rest detail behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-weekly-plan-experience/04-CONTEXT.md
@.planning/phases/04-weekly-plan-experience/04-RESEARCH.md
@.planning/phases/04-weekly-plan-experience/04-UI-SPEC.md
@.planning/phases/04-weekly-plan-experience/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add active-plan day lookup and route structure for modal detail</name>
  <files>lib/workouts/get-active-plan-day.ts, app/(app)/app/day/[dayId]/page.tsx, app/(app)/app/@modal/(.)day/[dayId]/page.tsx</files>
  <read_first>app/(app)/app/page.tsx, lib/current-user.ts, prisma/schema.prisma</read_first>
  <action>Create an active-plan day lookup helper that resolves a requested day for the current signed-in user only from the active plan. Add the route structure needed for a route-driven modal pattern, including the overlay route and the non-modal fallback page. If the day does not belong to the user's active plan, redirect or return not found according to the MVP contract.</action>
  <verify>test -f lib/workouts/get-active-plan-day.ts && test -f app/'(app)'/app/day/'[dayId]'/page.tsx && test -f app/'(app)'/app/@modal/'(.)day'/'[dayId]'/page.tsx</verify>
  <acceptance_criteria>
    - Active-plan-only lookup exists.
    - Route structure supports modal and direct-load cases.
    - Inactive or unauthorized day access is blocked.
  </acceptance_criteria>
  <done>The codebase can resolve detail views safely from active-plan data.</done>
</task>

<task type="auto">
  <name>Task 2: Build the workout/rest detail modal with next/previous navigation</name>
  <files>components/workouts/workout-detail-modal.tsx, app/(app)/app/@modal/(.)day/[dayId]/page.tsx</files>
  <read_first>components/workouts/weekly-plan-view.tsx, .planning/phases/04-weekly-plan-experience/04-UI-SPEC.md</read_first>
  <action>Create the detail modal UI for both workout and rest days. Workout detail should show day label, workout type, guidance, ordered exercise list, and descriptions. Rest days should use a simpler recovery-oriented state. Include clear close/back affordance and next/previous day navigation within the active plan.</action>
  <verify>test -f components/workouts/workout-detail-modal.tsx</verify>
  <acceptance_criteria>
    - Workout details include exercise descriptions and guidance.
    - Rest-day details render a simpler state.
    - Next/previous navigation exists.
    - The modal feels like an extension of the weekly plan rather than a disconnected page.
  </acceptance_criteria>
  <done>The user can inspect any day in the active plan without losing weekly context.</done>
</task>

<task type="auto">
  <name>Task 3: Add detail-route and modal tests</name>
  <files>tests/workouts/workout-detail-routing.test.ts, tests/workouts/workout-detail-modal.test.tsx</files>
  <read_first>components/workouts/workout-detail-modal.tsx, lib/workouts/get-active-plan-day.ts</read_first>
  <action>Add tests that verify detail routing is restricted to active-plan days, and that the modal content differs correctly between workout and rest days while still exposing ordered exercises, guidance, and next/previous navigation. Keep the tests at route/component contract level so they remain stable under future styling changes.</action>
  <verify>pnpm vitest run tests/workouts/workout-detail-routing.test.ts tests/workouts/workout-detail-modal.test.tsx</verify>
  <acceptance_criteria>
    - Detail tests exist and run.
    - The suite verifies active-plan routing protection.
    - The suite verifies workout-vs-rest modal behavior and navigation.
  </acceptance_criteria>
  <done>The modal detail experience is protected by focused tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm vitest run tests/workouts/workout-detail-routing.test.ts tests/workouts/workout-detail-modal.test.tsx` exits 0
- [ ] `pnpm lint` exits 0
- [ ] Detail routes never expose inactive-plan data
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app has a usable workout detail experience layered over the weekly plan
</success_criteria>

<output>
After completion, create `.planning/phases/04-weekly-plan-experience/04-02-SUMMARY.md`
</output>
